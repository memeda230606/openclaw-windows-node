<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClaw.SetupEngine.UI.Pages.PermissionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Disabled">

    <Grid RowDefinitions="Auto,*,Auto" Padding="48,28,48,28">

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="12" HorizontalAlignment="Center">
            <TextBlock Text="授予权限"
                       FontSize="28" FontWeight="SemiBold"
                       HorizontalAlignment="Center" />
            <TextBlock TextWrapping="Wrap" TextAlignment="Center"
                       FontSize="14" Opacity="0.7" MaxWidth="480"
                       HorizontalAlignment="Center"
                       Text="OpenClaw 在可以发送通知、访问摄像头和麦克风、捕获屏幕并读取位置时效果最佳。请在下方授予权限。" />
        </StackPanel>

        <!-- Permission rows -->
        <StackPanel x:Name="PermRows" Grid.Row="1" Spacing="8" Margin="0,24,0,0"
                    VerticalAlignment="Top" />

        <!-- Bottom: Refresh + Wizard + Next -->
        <StackPanel Grid.Row="2" Spacing="12">
            <Button x:Name="RefreshButton" HorizontalAlignment="Right"
                    Padding="12,8,12,8" Click="Refresh_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="14"
                               VerticalAlignment="Center" />
                    <TextBlock Text="刷新状态" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button Content="返回网关向导"
                    HorizontalAlignment="Stretch"
                    Height="40"
                    Click="BackToWizard_Click" />

            <Button Content="下一步"
                    HorizontalAlignment="Stretch"
                    Height="44" FontSize="15" FontWeight="SemiBold"
                    Click="Next_Click">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackground" Color="#60C8F8" />
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#52B0DA" />
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#469BBC" />
                    <SolidColorBrush x:Key="ButtonForeground" Color="Black" />
                    <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="Black" />
                    <SolidColorBrush x:Key="ButtonForegroundPressed" Color="Black" />
                </Button.Resources>
            </Button>
        </StackPanel>

    </Grid>
</Page>
